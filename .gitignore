node_modules/
.serena/

# Secrets and runtime agent state — never commit
.secrets/
# Authorized-agents registry: runtime state, self-initializes at deploy time.
# The *.example.json template is tracked; any real registry is not.
**/agent-registry.json
.claude/agent/

/node_modules
